import React, { Component } from 'react';

class Results extends Component {
  
  render() {
    return (
      <div className="results">
        <p>We will look at properties for containers and items.</p>
        <p>We will look at properties for containers and items.</p>
        <p>We will look at properties for containers and items.</p>
        <p>We will look at properties for containers and items.</p>
        <p>We will look at properties for containers and items.</p>
      </div>
    );
  }
}

export default Results;